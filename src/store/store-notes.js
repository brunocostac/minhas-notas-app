import Vue from 'vue'
import { db } from 'boot/database'

const state = {
    notes: {
    }
}

const mutations = {
}

const actions = {
}

const getters = {
}

export default {
    namespaced: true,
    state,
    mutations,
    actions,
    getters
}